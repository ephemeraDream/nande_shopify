{{ 'section-celebrity-swiper.css' | asset_url | stylesheet_tag }}

<div class="celebrity_swiper">
  <div class="padding-sider">
    <div class="celebrity_swiper_title">{{ section.settings.title }}</div>
    <div class="celebrity_swiper_text">{{ section.settings.text }}</div>
  </div>
  <div class="swiper">
    <div class="swiper-wrapper">
      {% for block in section.blocks %}
        <div class="swiper-slide celebrity_swiper_item">
          <div class="celebrity_swiper_item_left">
            <a
              {% if block.settings.link != blank %}
                href="{{ block.settings.link }}"
              {% endif %}
              class="celebrity_swiper_item_name"
            >
              {{- block.settings.name -}}
            </a>
            <div class="celebrity_swiper_item_intro">{{ block.settings.introduce }}</div>
            <div class="celebrity_swiper_item_desc">{{ block.settings.description }}</div>
          </div>
          <div class="celebrity_swiper_item_right">
            {% assign poster_image = blank %}
            {% if block.settings.cover_image != blank %}
              {% assign poster_image = block.settings.cover_image
                | image_url: width: block.settings.cover_image.width
              %}
            {% endif %}
            {% if block.settings.video_url != blank %}
              <video
                loop
                playsinline
                preload="auto"
                {% if poster_image != blank %}
                  poster="{{ poster_image }}"
                {% endif %}
                src="{{ block.settings.video_url }}"
              ></video>
            {% else %}
              {{
                block.settings.url_upload
                | video_tag:
                  autoplay: false,
                  loop: true,
                  muted: false,
                  playsinline: true,
                  preload: 'auto',
                  poster: poster_image
              }}
            {% endif %}
            <div class="celebrity_swiper_item_playicon">{{- 'icon-play.svg' | inline_asset_content -}}</div>
          </div>
        </div>
      {% endfor %}
    </div>
    <div class="celebrity_swiper_prev">{{- 'swiper-left.svg' | inline_asset_content -}}</div>
    <div class="celebrity_swiper_next">{{- 'swiper-right.svg' | inline_asset_content -}}</div>
  </div>
</div>

<script>
  new Swiper('.celebrity_swiper .swiper', {
    slidesPerView: 'auto',
    spaceBetween: 20,
    navigation: {
      nextEl: '.celebrity_swiper_next',
      prevEl: '.celebrity_swiper_prev',
    },
  });

  const playIcons = document.querySelectorAll('.celebrity_swiper .celebrity_swiper_item_playicon');

  playIcons.forEach((icon) => {
    icon.addEventListener('click', () => {
      const currentVideo = icon.closest('.celebrity_swiper_item').querySelector('video');

      if (currentVideo) {
        document.querySelectorAll('.celebrity_swiper_item video').forEach((video) => {
          if (video !== currentVideo) {
            video.pause();
          }
        });

        if (currentVideo.paused) {
          currentVideo.play();
        } else {
          currentVideo.pause();
        }
      }
    });
  });
</script>

{% schema %}
{
  "name": "celebrity swiper",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "richtext",
      "id": "title",
      "label": "title"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "text"
    }
  ],
  "blocks": [
    {
      "type": "swiper",
      "name": "swiper",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "name"
        },
        {
          "type": "richtext",
          "id": "introduce",
          "label": "introduce"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "description"
        },
        {
          "type": "text",
          "id": "link",
          "label": "link"
        },
        {
          "type": "image_picker",
          "id": "cover_image",
          "label": "video cover image"
        },
        {
          "type": "text",
          "id": "video_url",
          "label": "video url"
        },
        {
          "type": "video",
          "id": "url_upload",
          "label": "video upload"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "celebrity swiper",
      "blocks": [
        {
          "type": "swiper"
        }
      ]
    }
  ]
}
{% endschema %}
