{{ 'section-header-menu.css' | asset_url | stylesheet_tag }}

<style>
  #shopify-section-{{ section.id }} {
    z-index: 1001;
  }
</style>

<div class="header_menu_section session-box-padding">
  <div class="header_menu">
    {% for block in section.blocks %}
      {%- case block.type -%}
        {%- when 'single_link' -%}
          <div class="header_menu_item" data-type="single_link">
            <a
              class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
              {% if block.settings.link != blank %}
                href="{{ block.settings.link }}"
              {% endif %}
            >
              {{- block.settings.name -}}
            </a>
          </div>
        {%- when 'img_width_text' -%}
          <div class="header_menu_item" data-type="img_width_text">
            <a
              class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
              {% if block.settings.link != blank %}
                href="{{ block.settings.link }}"
              {% endif %}
            >
              {{- block.settings.name -}}
            </a>
            <div class="header_menu_item_submenu session-box-padding">
              <div class="header_menu_item_submenu_contain">
                {% assign page_index_list = 'one,two,thr,four,five,six' | split: ',' %}
                {%- for index in page_index_list -%}
                  {% capture image_id %}page_{{index}}_image{% endcapture %}
                  {% capture title_id %}page_{{index}}_title{% endcapture %}
                  {% capture text_id %}page_{{index}}_text{% endcapture %}
                  {% capture link_id %}page_{{index}}_link{% endcapture %}

                  {% assign image = block.settings[image_id] %}
                  {% assign title = block.settings[title_id] %}
                  {% assign text = block.settings[text_id] %}
                  {% assign url = block.settings[link_id] %}
                  {% if image != blank %}
                    <a
                      href="{{ url }}"
                      class="header_menu_item_submenu_item"
                    >
                      <div class="header_menu_item_submenu_item_img">
                        <img src="{{ image | image_url }}">
                        {{ 'icon-img-right-arrow.svg' | inline_asset_content }}
                      </div>
                      <div class="header_menu_item_submenu_item_title">{{ title }}</div>
                      <div class="header_menu_item_submenu_item_desc">{{ text }}</div>
                    </a>
                  {% endif %}
                {%- endfor -%}
              </div>
            </div>
          </div>
        {%- when 'link_list' -%}
          <div class="header_menu_item" data-type="link_list">
            <a
              class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
              {% if block.settings.link != blank %}
                href="{{ block.settings.link }}"
              {% endif %}
            >
              {{- block.settings.name -}}
            </a>
            <div class="header_menu_item_submenu">
              {%- if block.settings.menu != blank -%}
                {%- for link in block.settings.menu.links -%}
                  <a
                    href="{{ link.url }}"
                    class="header_menu_item_submenu_item{% if link.active %} header_menu_item_submenu_item_active{% endif %}"
                  >
                    {{ link.title | escape }}
                  </a>
                {%- endfor -%}
              {%- endif -%}
            </div>
          </div>
        {%- when 'product_collection' -%}
          <div class="header_menu_item" data-type="product_collection">
            <a
              class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
              {% if block.settings.link != blank %}
                href="{{ block.settings.link }}"
              {% endif %}
            >
              {{- block.settings.name -}}
            </a>
            <div class="header_menu_item_submenu session-box-padding">
              <div class="header_menu_item_submenu_contain">
                <div class="header_menu_item_submenu_contain_left">
                  <div class="header_menu_item_submenu_tabhead">
                    {%- if block.settings.collection_list != blank -%}
                      {%- for item in block.settings.collection_list -%}
                        <a
                          href="{{ item.url }}"
                          class="header_menu_item_submenu_item{% if forloop.first %} header_menu_item_submenu_item_active{% endif %}"
                          data-id="{{ item.handle }}"
                        >
                          {{ item.title }}
                        </a>
                      {%- endfor -%}
                    {%- endif -%}
                  </div>
                  <div class="header_menu_item_submenu_info">
                    <div class="header_menu_item_submenu_info_title">{{ block.settings.info_title }}</div>
                    <div class="header_menu_item_submenu_info_desc">{{ block.settings.info_text }}</div>
                  </div>
                </div>
                <div class="header_menu_item_submenu_contain_right">
                  {%- if block.settings.collection_list != blank -%}
                    {%- for item in block.settings.collection_list -%}
                      <div
                        class="header_menu_item_submenu_tabcontent{% if forloop.first %} show{% endif %}"
                        data-id="{{ item.handle }}"
                      >
                        <div
                          class="swiper"
                        >
                          <div class="swiper-wrapper">
                            {% for product in item.products %}
                              {% assign show_info = product.metafields.custom.header_menu_info.value %}
                              <a class="swiper-slide" href="{{ product.url }}">
                                <div class="header_menu_item_submenu_product_img">
                                  <img src="{{ product.featured_image | image_url }}">
                                  {% if show_info.tag.value != blank %}
                                    <div class="header_menu_item_submenu_product_tag">{{ show_info.tag.value }}</div>
                                  {% endif %}
                                </div>
                                <div class="header_menu_item_submenu_product_info">
                                  <div>
                                    <h4 class="header_menu_item_submenu_product_title">
                                      {% if show_info.title.value != blank %}
                                        {{ show_info.title.value }}
                                      {% else %}
                                        Maidesite
                                      {% endif %}
                                    </h4>

                                    <div class="header_menu_item_submenu_product_desc text-body">
                                      {% if show_info.description.value != blank %}
                                        {{ show_info.description | metafield_tag }}
                                      {% else %}
                                        {{ product.title }}
                                      {% endif %}
                                    </div>
                                  </div>
                                  <div>
                                    {% render 'product-rating', product: product %}
                                    <h4 class="header_menu_item_submenu_product_price">
                                      {{ product.price | money_without_trailing_zeros }}
                                    </h4>
                                  </div>
                                </div>
                              </a>
                            {% endfor %}
                          </div>
                        </div>
                        <div class="header_menu_item_submenu_swiper_prev">
                          {{ 'header-swiper-left.svg' | inline_asset_content }}
                        </div>
                        <div class="header_menu_item_submenu_swiper_next">
                          {{ 'header-swiper-right.svg' | inline_asset_content }}
                        </div>
                      </div>
                    {%- endfor -%}
                  {%- endif -%}
                </div>
              </div>
            </div>
          </div>
      {% endcase %}
    {% endfor %}
  </div>
</div>
<div class="header_menu_mobile">
  {% for block in section.blocks %}
    {%- case block.type -%}
      {%- when 'single_link' -%}
        <div class="header_menu_item" data-type="single_link">
          <a
            class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
            {% if block.settings.link != blank %}
              href="{{ block.settings.link }}"
            {% endif %}
          >
            {{- block.settings.name -}}
          </a>
        </div>
      {%- when 'img_width_text' -%}
        <div class="header_menu_item" data-type="img_width_text">
          <a
            class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
            {% if block.settings.link != blank %}
              href="{{ block.settings.link }}"
            {% endif %}
          >
            {{- block.settings.name -}}
          </a>
          <div class="header_menu_item_icon">{{- 'swiper-right.svg' | inline_asset_content -}}</div>
          <div class="header_menu_item_submenu session-box-padding">
            <div class="header_menu_item_submenu_title">
              {{- 'swiper-left.svg' | inline_asset_content -}}
              {{- block.settings.name -}}
            </div>
            <div class="header_menu_item_submenu_contain">
              {% assign page_index_list = 'one,two,thr,four,five,six' | split: ',' %}
              {%- for index in page_index_list -%}
                {% capture image_id %}page_{{ index }}_image{% endcapture %}
                {% capture title_id %}page_{{ index }}_title{% endcapture %}
                {% capture text_id %}page_{{ index }}_text{% endcapture %}
                {% capture link_id %}page_{{ index }}_link{% endcapture %}

                {% assign image = block.settings[image_id] %}
                {% assign title = block.settings[title_id] %}
                {% assign text = block.settings[text_id] %}
                {% assign url = block.settings[link_id] %}
                {% if image != blank %}
                  <a
                    href="{{ url }}"
                    class="header_menu_item_submenu_item"
                  >
                    <div class="header_menu_item_submenu_item_img">
                      <img src="{{ image | image_url }}">
                      {{ 'icon-img-right-arrow.svg' | inline_asset_content }}
                    </div>
                    <div class="header_menu_item_submenu_item_title">{{ title }}</div>
                    <div class="header_menu_item_submenu_item_desc">{{ text }}</div>
                  </a>
                {% endif %}
              {%- endfor -%}
            </div>
          </div>
        </div>
      {%- when 'link_list' -%}
        <div class="header_menu_item" data-type="link_list">
          <a
            class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
            {% if block.settings.link != blank %}
              href="{{ block.settings.link }}"
            {% endif %}
          >
            {{- block.settings.name -}}
          </a>
          <div class="header_menu_item_icon">{{- 'swiper-right.svg' | inline_asset_content -}}</div>
          <div class="header_menu_item_submenu">
            <div class="header_menu_item_submenu_title">
              {{- 'swiper-left.svg' | inline_asset_content -}}
              {{- block.settings.name -}}
            </div>
            {%- if block.settings.menu != blank -%}
              {%- for link in block.settings.menu.links -%}
                <a
                  href="{{ link.url }}"
                  class="header_menu_item_submenu_item{% if link.active %} header_menu_item_submenu_item_active{% endif %}"
                >
                  {{ link.title | escape }}
                </a>
              {%- endfor -%}
            {%- endif -%}
          </div>
        </div>
      {%- when 'product_collection' -%}
        <div class="header_menu_item" data-type="product_collection">
          <a
            class="header_menu_item_title{% if block.settings.link.active %} header_menu_item_title_active{% endif %}"
            {% if block.settings.link != blank %}
              href="{{ block.settings.link }}"
            {% endif %}
          >
            {{- block.settings.name -}}
          </a>
          <div class="header_menu_item_icon">{{- 'swiper-right.svg' | inline_asset_content -}}</div>
          <div class="header_menu_item_submenu session-box-padding">
            <div class="header_menu_item_submenu_title">
              {{- 'swiper-left.svg' | inline_asset_content -}}
              {{- block.settings.name -}}
            </div>
            <div class="header_menu_item_submenu_contain">
              <div class="header_menu_item_submenu_contain_left">
                <div class="header_menu_item_submenu_tabhead">
                  {%- if block.settings.collection_list != blank -%}
                    {%- for item in block.settings.collection_list -%}
                      <a
                        class="header_menu_item_submenu_item{% if forloop.first %} header_menu_item_submenu_item_active{% endif %}"
                        data-id="{{ item.handle }}"
                      >
                        {{ item.title }}
                      </a>
                    {%- endfor -%}
                  {%- endif -%}
                </div>
                <div class="header_menu_item_submenu_info">
                  <div class="header_menu_item_submenu_info_title">{{ block.settings.info_title }}</div>
                  <div class="header_menu_item_submenu_info_desc">{{ block.settings.info_text }}</div>
                </div>
              </div>
              <div class="header_menu_item_submenu_contain_right">
                {%- if block.settings.collection_list != blank -%}
                  {%- for item in block.settings.collection_list -%}
                    <div
                      class="header_menu_item_submenu_tabcontent{% if forloop.first %} show{% endif %}"
                      data-id="{{ item.handle }}"
                    >
                      <div
                        class="swiper"
                      >
                        <div class="swiper-wrapper">
                          {% for product in item.products %}
                            {% assign show_info = product.metafields.custom.header_menu_info.value %}
                            <a class="swiper-slide" href="{{ product.url }}">
                              <div class="header_menu_item_submenu_product_img">
                                <img src="{{ product.featured_image | image_url }}">
                                {% if show_info.tag.value != blank %}
                                  <div class="header_menu_item_submenu_product_tag">{{ show_info.tag.value }}</div>
                                {% endif %}
                              </div>
                              <div class="header_menu_item_submenu_product_info">
                                <div>
                                  <h4 class="header_menu_item_submenu_product_title">
                                    {% if show_info.title.value != blank %}
                                      {{ show_info.title.value }}
                                    {% else %}
                                      Maidesite
                                    {% endif %}
                                  </h4>

                                  <div class="header_menu_item_submenu_product_desc text-body">
                                    {% if show_info.description.value != blank %}
                                      {{ show_info.description | metafield_tag }}
                                    {% else %}
                                      {{ product.title }}
                                    {% endif %}
                                  </div>
                                </div>
                                <div>
                                  {% render 'product-rating', product: product %}
                                  <h4 class="header_menu_item_submenu_product_price">
                                    {{ product.price | money_without_trailing_zeros }}
                                  </h4>
                                </div>
                              </div>
                            </a>
                          {% endfor %}
                        </div>
                      </div>
                      <div class="header_menu_item_submenu_swiper_prev">
                        {{ 'header-swiper-left.svg' | inline_asset_content }}
                      </div>
                      <div class="header_menu_item_submenu_swiper_next">
                        {{ 'header-swiper-right.svg' | inline_asset_content }}
                      </div>
                      <a
                        href="{{ item.url }}"
                        class="header_menu_item_submenu_item_viewall"
                      >
                        View All
                        {{ 'icon-right-arrow.svg' | inline_asset_content -}}
                      </a>
                    </div>
                  {%- endfor -%}
                {%- endif -%}
              </div>
            </div>
          </div>
        </div>
    {% endcase %}
  {% endfor %}
</div>

<script>
  (() => {
    const product_collection_menus = document.querySelectorAll(
      ".header_menu_section .header_menu_item[data-type='product_collection']"
    );
    product_collection_menus.forEach((menu) => {
      const product_collections = menu.querySelectorAll('.header_menu_item_submenu_tabcontent');
      product_collections.forEach((item) => {
        const swiperWrapper = item.querySelector('.swiper .swiper-wrapper');
        const slides = swiperWrapper.querySelectorAll('.swiper-slide');
        {% comment %} const perView = 3;

        if (slides.length < perView * 2) {
          const cloneCount = perView * 2 - slides.length;
          for (let i = 0; i < cloneCount; i++) {
            swiperWrapper.appendChild(slides[i % slides.length].cloneNode(true));
          }
        } {% endcomment %}
        new Swiper(item.querySelector('.swiper'), {
          slidesPerView: 3,
          spaceBetween: 30,
          slidesPerGroup: 3,
          {% comment %} loop: true,
          loopFillGroupWithBlank: true, {% endcomment %}
          navigation: {
            nextEl: item.querySelector('.header_menu_item_submenu_swiper_next'),
            prevEl: item.querySelector('.header_menu_item_submenu_swiper_prev'),
          },
          observer: true,
        });
      });

      const tabs_item = menu.querySelectorAll('.header_menu_item_submenu_item');
      tabs_item.forEach((item) => {
        item.addEventListener('mouseenter', () => {
          menu
            .querySelector('.header_menu_item_submenu_item_active')
            .classList.remove('header_menu_item_submenu_item_active');
          item.classList.add('header_menu_item_submenu_item_active');
          const dataId = item.getAttribute('data-id');
          menu.querySelector('.header_menu_item_submenu_tabcontent.show').classList.remove('show');
          menu.querySelector(`.header_menu_item_submenu_tabcontent[data-id="${dataId}"]`).classList.add('show');
        });
      });
    });

    const header_menu_item = document.querySelectorAll('.header_menu_mobile .header_menu_item');
    header_menu_item.forEach((menu) => {
      const submenu = menu.querySelector('.header_menu_item_submenu');
      menu.addEventListener('click', (e) => {
        if (e.target.closest('.header_menu_item_title')) {
          return;
        }
        if (submenu) {
          submenu.style.display = 'block';
        }
      });

      if (submenu) {
        submenu.querySelector('.header_menu_item_submenu_title').addEventListener('click', (e) => {
          e.stopPropagation();
          submenu.style.display = 'none';
        });
      }
    });

    const product_collection_menus_mobile = document.querySelectorAll(
      ".header_menu_mobile .header_menu_item[data-type='product_collection']"
    );
    product_collection_menus_mobile.forEach((menu) => {
      const product_collections = menu.querySelectorAll('.header_menu_item_submenu_tabcontent');
      product_collections.forEach((item) => {
        new Swiper(item.querySelector('.swiper'), {
          slidesPerView: 1.1,
          spaceBetween: 10,
          loop: false,
          observer: true,
        });
      });

      const tabs_item = menu.querySelectorAll('.header_menu_item_submenu_item');
      tabs_item.forEach((item) => {
        item.addEventListener('mouseenter', () => {
          menu
            .querySelector('.header_menu_item_submenu_item_active')
            .classList.remove('header_menu_item_submenu_item_active');
          item.classList.add('header_menu_item_submenu_item_active');
          const dataId = item.getAttribute('data-id');
          menu.querySelector('.header_menu_item_submenu_tabcontent.show').classList.remove('show');
          menu.querySelector(`.header_menu_item_submenu_tabcontent[data-id="${dataId}"]`).classList.add('show');
        });
      });
    });

    document.querySelector('.header_simple_hamburger').addEventListener('click', (e) => {
      const item = e.target.closest('.header_simple_hamburger');
      const header_menu_mobile = document.querySelector('.header_menu_mobile');
      const parent = header_menu_mobile.closest('.shopify-section-group-header-group');
      item.classList.toggle('active');
      if (item.classList.contains('active')) {
        header_menu_mobile.style.display = 'block';
        document.body.style.overflowY = 'hidden';
        parent.style.top = '0';
        parent.style.zIndex = '999';
      } else {
        header_menu_mobile.style.display = 'none';
        document.body.style.overflowY = 'auto';
      }
    });
  })();
</script>

{% schema %}
{
  "name": "header menu",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["footer"],
    "templates": ["*"]
  },
  "settings": [],
  "blocks": [
    {
      "type": "single_link",
      "name": "single link",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "name"
        },
        {
          "type": "url",
          "id": "link",
          "label": "link"
        }
      ]
    },
    {
      "type": "img_width_text",
      "name": "img width text",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "name"
        },
        {
          "type": "url",
          "id": "link",
          "label": "link"
        },
        {
          "type": "image_picker",
          "id": "page_one_image",
          "label": "page one image"
        },
        {
          "type": "text",
          "id": "page_one_title",
          "label": "page one title"
        },
        {
          "type": "textarea",
          "id": "page_one_text",
          "label": "page one text"
        },
        {
          "type": "url",
          "id": "page_one_link",
          "label": "page one link"
        },
        {
          "type": "image_picker",
          "id": "page_two_image",
          "label": "page two image"
        },
        {
          "type": "text",
          "id": "page_two_title",
          "label": "page two title"
        },
        {
          "type": "textarea",
          "id": "page_two_text",
          "label": "page two text"
        },
        {
          "type": "url",
          "id": "page_two_link",
          "label": "page two link"
        },
        {
          "type": "image_picker",
          "id": "page_thr_image",
          "label": "page thr image"
        },
        {
          "type": "text",
          "id": "page_thr_title",
          "label": "page thr title"
        },
        {
          "type": "textarea",
          "id": "page_thr_text",
          "label": "page thr text"
        },
        {
          "type": "url",
          "id": "page_thr_link",
          "label": "page thr link"
        },
        {
          "type": "image_picker",
          "id": "page_four_image",
          "label": "page four image"
        },
        {
          "type": "text",
          "id": "page_four_title",
          "label": "page four title"
        },
        {
          "type": "textarea",
          "id": "page_four_text",
          "label": "page four text"
        },
        {
          "type": "url",
          "id": "page_four_link",
          "label": "page four link"
        },
        {
          "type": "image_picker",
          "id": "page_five_image",
          "label": "page five image"
        },
        {
          "type": "text",
          "id": "page_five_title",
          "label": "page five title"
        },
        {
          "type": "textarea",
          "id": "page_five_text",
          "label": "page five text"
        },
        {
          "type": "url",
          "id": "page_five_link",
          "label": "page five link"
        },
        {
          "type": "image_picker",
          "id": "page_six_image",
          "label": "page six image"
        },
        {
          "type": "text",
          "id": "page_six_title",
          "label": "page six title"
        },
        {
          "type": "textarea",
          "id": "page_six_text",
          "label": "page six text"
        },
        {
          "type": "url",
          "id": "page_six_link",
          "label": "page six link"
        }
      ]
    },
    {
      "type": "link_list",
      "name": "link list",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "name"
        },
        {
          "type": "url",
          "id": "link",
          "label": "link"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "menu"
        }
      ]
    },
    {
      "type": "product_collection",
      "name": "product collection",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "name"
        },
        {
          "type": "url",
          "id": "link",
          "label": "link"
        },
        {
          "type": "collection_list",
          "id": "collection_list",
          "label": "collection list"
        },
        {
          "type": "richtext",
          "id": "info_title",
          "label": "info title"
        },
        {
          "type": "richtext",
          "id": "info_text",
          "label": "info text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "header menu"
    }
  ]
}
{% endschema %}
